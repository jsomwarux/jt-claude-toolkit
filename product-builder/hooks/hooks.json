{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && npx --no-install prettier --write \"$f\" >/dev/null 2>&1; true; }"
          }
        ]
      }
    ]
  }
}
